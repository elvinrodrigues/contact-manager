## models -> data strucutres(contact, category)
## handlers -> HTTP endpoints
## serivces -> business logic
## repository -> database queries
## migrations -> database schema

### For the import statement --> `_ "github.com/lib/pq"` in db.go do `go get github.com/lib/pq`